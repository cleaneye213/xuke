1、两个线程，取钱线程和存钱线程，要求存款者和取钱者不断的重复存款和取钱（50次）
   要求当存款者将钱存入指定账户时，取款者就立即把钱取出，
   不允许存款者重复存款，也不允许取钱者重复取钱。
   
   步骤：
   1、创建用户Account对象（账号，余额），方法：取钱，存钱。
	注意：存取钱时，可能会用到的线程方法wait()等待,唤醒其他进程：notifyAll()
   2、创建取钱线程类，模拟50次重复取钱动作。
   3、创建存钱线程类，模拟50次重复存钱动作。
   4、创建测试类，实例一个账号，创建一个取钱者、两个存钱者线程类，对账号进程存取
	钱。