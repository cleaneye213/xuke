1、编写一个学生类，提供name,age,phone,address,email成员变量
，且未每个成员变量提供getter及setter方法。并创建学生类行为方法如：上课、考试等;为学生类提供默认的构造器和带所有成员变量的构造器。
2、利用上面创建的学生类，定义一个数组，保存多个student对象作为通讯录数据，程序可通过name，eamil、address查询，如果找不到，则友好提示。
3、在查询方法中，增加分支控制，如果是"校长"的话，可以查询
全部字段，是"主任"-->查询name,eamil，是"老师"-->查询name,address
解答：1、方法一、增加一个字符串，用来标识职位，根据职位的不同，调用不
		通的查询方法；
	  2、方法二、新增一个教师类，包含职位属性，创建一个检查
	  学生信息的方法，根据职位的不同，查询的内容不同。